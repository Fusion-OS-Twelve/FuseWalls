import 'package:flutter/material.dart';

Color getMonetBGColor(context) {
  return Theme.of(context).colorScheme.background;
}

Color getMonetOnBGColor(context) {
  return Theme.of(context).colorScheme.onBackground;
}
